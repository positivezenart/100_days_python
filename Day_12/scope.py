"""Global scope variables can be accessed globally anywhwhere, but local scope variables can only be accessed within a function.
if, loop or in while loop defined variables are always global, In order to alter global scope variables, you need to declare them as global
inside function before using same variable for altering global scope variables"""